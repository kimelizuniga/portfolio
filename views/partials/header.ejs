<!DOCTYPE html>
<html>
    <head>
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>KZ Portfolio</title>
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
        <link rel="stylesheet" href="stylesheets/main.css">
    </head>
    <body>
        <nav id="navProp" class="navbar navbar-expand-lg mb-3">
            <a class="navbar-brand" href="/"><img class="myLogo" src="/images/logo (1).png"></a>
            <% if(currentUser){ %>
            <a href="/">Signed in as <%= currentUser.username %></a>
            <% } %>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navBarLinks" aria-controls="navBarLinks" aria-expanded="false" aria-label="Toggle Navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse  justify-content-center" id="navBarLinks">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link rounded" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link rounded" href="/projects">Projects</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link rounded" href="/resume">Resume</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link rounded" href="/about">About</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link rounded"  href="/contact">Contact</a>
                    </li>
                </ul>
            </div>
        </nav>
        <div class="container">
            <%if(error && error.length > 0){%>
            <div class="alert alert-danger"><%=error%></div>    
            <%}%>
        </div>
        <div class="container">
            <%if(success && success.length > 0){%>
            <div class="alert alert-success"><%=success%></div>   
            <%}%> 
        </div>

   